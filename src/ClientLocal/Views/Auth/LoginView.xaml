<Window x:Class="ClientLocal.Views.Auth.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="330" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Iniciar sesión"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Correo"/>
            <TextBox x:Name="CorreoTextBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Contraseña"/>
            <PasswordBox x:Name="ContrasenaPasswordBox" Height="30"/>
        </StackPanel>

        <TextBlock x:Name="EstadoTextBlock"
                   Grid.Row="3"
                   Foreground="Red"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Registrarse"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="IrARegistro_Click"/>
            <Button Content="Entrar"
                    Width="100"
                    Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Window>